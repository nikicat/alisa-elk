__pycache__/
*.py[cod]
.venv/
.env
*.db
*.db-journal
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# Bind-mount target — directory itself is tracked via .gitkeep so a clone
# is ready for `docker compose up`, contents are not.
data/*
!data/.gitkeep
